// Twitter / X uses the OpenGraph image by default; this file ensures the
// summary_large_image card renders the same brand mark even on platforms
// that strictly look for twitter:image.
export { default, alt, size, contentType } from "./opengraph-image";
